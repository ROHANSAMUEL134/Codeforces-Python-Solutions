for _ in range(int(input())):
    n = int(input())
    arr = list(map(int,input().split()))
    dic = {}
    for i in arr:
        if i in dic: dic[i] += 1
        else: dic[i] = 1
        if dic[i] == 3:
            print(i)
            break
    else:
        print(-1)
